# Bu dosya projemizde kullanacağımız tüm kütüphaneleri içerir
# Terminalde 'pip install -r requirements.txt' yazınca hepsi otomatik yüklenir

streamlit==1.28.0          # Web arayüzü için
google-generativeai==0.3.0  # Google Gemini AI için
langchain==0.0.346          # RAG sistemi için
chromadb==0.4.15           # Vektör veritabanı
python-dotenv==1.0.0       # Gizli bilgileri yönetmek için
pypdf==3.17.0              # PDF dosyalarını okumak için
